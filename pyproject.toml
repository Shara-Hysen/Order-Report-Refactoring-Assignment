[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "order-report"
version = "0.1.0"
dependencies = [
    "pandas",
]

[tool.setuptools.packages.find]
where = ["src"]